[project]
name = "booot"
version = "0.1.0"
description = "University service desk bot core logic"
readme = "README.md"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
